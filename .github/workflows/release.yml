name: Release

on:
  push:
    branches:
      - master
    tags:
      - "v*.*.*"

jobs:
  release-image:
    permissions:
      contents: read
      id-token: write
      packages: write
    uses: capnspacehook/go-workflows/.github/workflows/release-image.yml@master
